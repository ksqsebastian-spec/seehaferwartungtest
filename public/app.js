const $=s=>document.querySelector(s);const reduced=matchMedia('(prefers-reduced-motion: reduce)');let motion=!reduced.matches;const video=$('#hero-video');const toggle=$('#motion-toggle');
function applyMotion(){document.body.classList.toggle('motion-off',!motion);toggle.textContent=motion?'Ⅱ':'▷';toggle.setAttribute('aria-label',motion?'Animationen pausieren':'Animationen abspielen');if(motion)video.play().catch(()=>{});else {video.pause();$('.hero-media').style.transform='none';$('.hero-copy').style.opacity='1'}}
applyMotion();toggle.addEventListener('click',()=>{motion=!motion;applyMotion()});reduced.addEventListener('change',e=>{motion=!e.matches;applyMotion()});document.documentElement.classList.add('js-motion');const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));
const states=['Termin abgestimmt. Alles im Blick.','Hausmeister informiert. Zugang geklärt.','Türen gewartet. Protokoll bereit.','Ein Ansprechpartner. Weniger Aufwand.'];let statusIndex=0;setInterval(()=>{if(!motion||document.hidden)return;const el=$('#status-text');el.classList.add('changing');setTimeout(()=>{el.textContent=states[++statusIndex%states.length];el.classList.remove('changing')},350)},4200);
let frame=false;addEventListener('scroll',()=>{if(frame||!motion)return;frame=true;requestAnimationFrame(()=>{const y=window.scrollY;$('.hero-media').style.transform=`translateY(${Math.min(y*.22,220)}px)`;$('.hero-copy').style.opacity=Math.max(0,1-y/(innerHeight*.8));const r=$('.peace').getBoundingClientRect();const shift=Math.max(-35,Math.min(35,(innerHeight/2-r.top)*.04));$('.photo-one').style.transform=`translateY(${-shift}px) rotate(-6deg)`;$('.photo-two').style.transform=`translateY(${shift}px) rotate(7deg)`;frame=false})},{passive:true});
const menu=$('#menu'),menuToggle=$('.menu-toggle');function closeMenu(){menu.hidden=true;menuToggle.setAttribute('aria-expanded','false');menuToggle.textContent='Menü'}menuToggle.addEventListener('click',()=>{menu.hidden=!menu.hidden;menuToggle.setAttribute('aria-expanded',String(!menu.hidden));menuToggle.textContent=menu.hidden?'Menü':'Schließen'});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()});document.addEventListener('click',e=>{if(!menu.hidden&&!menu.contains(e.target)&&!menuToggle.contains(e.target))closeMenu()});
const inquiry=$('#inquiry');function openInquiry(email=''){closeMenu();$('#inquiry-form').hidden=false;$('#email-ready').hidden=true;$('#copy-status').textContent='';if(email)$('#inquiry-form [name=email]').value=email;inquiry.showModal()}
document.querySelectorAll('[data-inquiry]').forEach(b=>b.addEventListener('click',()=>openInquiry()));$('.email-start').addEventListener('submit',e=>{e.preventDefault();openInquiry($('#hero-email').value)});document.querySelectorAll('dialog .close').forEach(b=>b.addEventListener('click',()=>b.closest('dialog').close()));document.querySelectorAll('dialog').forEach(d=>d.addEventListener('click',e=>{if(e.target===d){const r=d.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)d.close()}}));
let inquiryText='';$('#inquiry-form').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.target);inquiryText=`Guten Tag Herr Blöhse,\n\nich interessiere mich für die Wartung unseres Objekts.\n\nE-Mail: ${d.get('email')}\nName / Verwaltung: ${d.get('name')||'Noch offen'}\nObjektadresse: ${d.get('address')||'Noch offen'}\n\n${d.get('message')||'Bitte kontaktieren Sie mich für eine erste Abstimmung.'}\n\nMit freundlichen Grüßen\n${d.get('name')||''}`;const href='mailto:t.bloehse@seehafer-elemente.de?subject='+encodeURIComponent('Unverbindliche Wartungsanfrage')+'&body='+encodeURIComponent(inquiryText);$('#mail-link').href=href;$('#inquiry-form').hidden=true;$('#email-ready').hidden=false;$('#mail-link').focus();window.location.href=href});$('#copy-inquiry').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(inquiryText);$('#copy-status').textContent='Anfragetext kopiert.'}catch{$('#copy-status').textContent='Kopieren nicht möglich. Bitte nutzen Sie „E-Mail öffnen“.'}});
for(const id of ['sample-open','sample-footer'])$('#'+id).addEventListener('click',()=>$('#sample').showModal());$('#print-sample').addEventListener('click',()=>window.print());$('#privacy-open').addEventListener('click',()=>$('#privacy').showModal());
