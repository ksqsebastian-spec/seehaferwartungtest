import http from 'node:http';import fs from 'node:fs';import path from 'node:path';
const types={'.html':'text/html','.css':'text/css','.js':'text/javascript','.webp':'image/webp','.mp4':'video/mp4','.ttf':'font/ttf','.svg':'image/svg+xml'};
http.createServer((req,res)=>{const p=path.resolve('public','.'+decodeURIComponent(new URL(req.url,'http://localhost').pathname));if(!p.startsWith(path.resolve('public')+'/')&&p!==path.resolve('public')){res.writeHead(403).end();return}const file=p===path.resolve('public')?p+'/index.html':p;fs.stat(file,(e,s)=>{if(e||!s.isFile()){res.writeHead(404).end('Not found');return}res.setHeader('Content-Type',types[path.extname(file)]||'application/octet-stream');res.setHeader('Accept-Ranges','bytes');const range=req.headers.range?.match(/bytes=(\d+)-(\d*)/);if(range){const start=+range[1],end=range[2]?Math.min(+range[2],s.size-1):s.size-1;if(start>=s.size){res.writeHead(416).end();return}res.writeHead(206,{'Content-Range':`bytes ${start}-${end}/${s.size}`,'Content-Length':end-start+1});fs.createReadStream(file,{start,end}).pipe(res)}else{res.setHeader('Content-Length',s.size);fs.createReadStream(file).pipe(res)}})}).listen(4318,'0.0.0.0',()=>console.log('http://localhost:4318'));
